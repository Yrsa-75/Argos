# Workflow désactivé — Cloudflare Pages déploie automatiquement depuis GitHub.
name: Deploy (disabled)
on:
  workflow_dispatch:
  jobs:
    deploy:
        runs-on: ubuntu-latest
            if: false
                steps:
                      - run: echo "disabled"
